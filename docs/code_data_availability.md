# Code and Data Availability

This repository contains a standalone reproducibility release of the synthetic `v3_health` healthcare benchmark.

All experiments use fully synthetic data generated by the SCM in this repository. No real patient records are included.

## Repository and Provenance

- Repository: `v3-health-benchmark`
- Source project: `causal-agent-benchmark`
- Source branch: `V2-healthcare`
- Source commit: `7c08b6098c65045c7455fe8f44824bbb85e77dd4`
- Extraction provenance: `PROVENANCE.md`

## License

Reuse, redistribution, and commercial use are governed by `LICENSE`.

## Reproduction

Install:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements-lock.txt
pip install -e . --no-deps --no-build-isolation
```

Run:

```bash
v3-health run --config configs/v3_health_default.yaml --output-dir results
v3-health dashboard --results results/results_summary.json --output results/dashboard.html
```

Generate config-derived tables:

```bash
PYTHONPATH=src python3 scripts/tables/generate_scenario_spec_table.py \
  --config configs/v3_health_default.yaml \
  --output-dir outputs/tables

PYTHONPATH=src python3 scripts/tables/generate_agent_spec_table.py \
  --config configs/v3_health_default.yaml \
  --output-dir outputs/tables
```

## Artifact Locations

- Canonical config: `configs/v3_health_default.yaml`
- Scenario configs: `configs/scenarios/`
- Agent configs: `configs/agents/`
- Release snapshot: `artifacts/release/`
- Local benchmark outputs: `results/`
- Generated specification tables: `outputs/tables/`
